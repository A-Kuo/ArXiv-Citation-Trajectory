requests==2.31.0
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.2
textstat==0.7.3
pyarrow==14.0.1
scipy>=1.10.0

# Database — PostgreSQL primary, MySQL/SQLServer compatible via SQLAlchemy
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0   # PostgreSQL driver
# pymysql>=1.1.0          # MySQL driver (uncomment if using MySQL)
# pyodbc>=5.0.0           # SQL Server driver (uncomment if using SQL Server)

# Phase 3 — modelling (regression + boosting)
xgboost>=2.0.0
lightgbm>=4.0.0

# Phase 4 — interactive dashboard
streamlit==1.31.1
plotly==5.18.0

# Phase 5 — Testing and API
pytest>=7.0.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
httpx>=0.24.0  # For testing FastAPI endpoints

# Phase 6 — RAG paper Q&A (retrieval + generation)
rank-bm25>=0.2.2
anthropic>=0.34.0            # generation (requires ANTHROPIC_API_KEY at call time)
# sentence-transformers is optional: rag_embeddings.py falls back
# automatically to an offline TF-IDF+SVD backend when it's absent or the
# model hub is unreachable. Install it for real semantic embeddings:
# sentence-transformers>=2.2.0
# Vector search is brute-force numpy cosine similarity (fine at this
# corpus scale); swap in faiss-cpu in rag_indexing.py if the index grows
# past ~100k chunks and search latency becomes a problem.
